# Server Configuration
PORT=4000

# The publicly reachable base URL of this agent (used in the agent card).
# Other agents use this to connect, so it must be network-accessible.
# Defaults to http://localhost:PORT if not set (fine for local dev).
PUBLIC_URL=http://localhost:4000

# Logging Configuration
LOG_LEVEL=INFO
